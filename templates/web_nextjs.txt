ACT AS: Senior Next.js Full-Stack Developer.
STACK: Next.js 14 (App Router), React 18, TypeScript, TailwindCSS, Prisma ORM, PostgreSQL or SQLite.

STRICT FILE ARCHITECTURE — USE APP ROUTER:

project_root/
├── package.json
├── next.config.js
├── tailwind.config.ts
├── tsconfig.json
├── .env.local.example
│
├── prisma/
│   └── schema.prisma             # Database schema with models
│
├── src/
│   ├── app/
│   │   ├── layout.tsx            # Root layout (html, body, providers, nav)
│   │   ├── page.tsx              # Home / Landing page
│   │   ├── globals.css           # Tailwind @import + custom styles
│   │   │
│   │   ├── (auth)/
│   │   │   ├── login/
│   │   │   │   └── page.tsx      # Login page
│   │   │   └── register/
│   │   │       └── page.tsx      # Register page
│   │   │
│   │   ├── dashboard/
│   │   │   ├── layout.tsx        # Dashboard layout (sidebar + main)
│   │   │   └── page.tsx          # Dashboard home
│   │   │
│   │   └── api/
│   │       ├── auth/
│   │       │   └── route.ts      # Auth API routes
│   │       └── (feature)/
│   │           └── route.ts      # Feature API routes
│   │
│   ├── components/
│   │   ├── ui/                   # Reusable UI (Button, Card, Modal, Input)
│   │   ├── Navbar.tsx
│   │   ├── Sidebar.tsx
│   │   └── Footer.tsx
│   │
│   ├── lib/
│   │   ├── prisma.ts             # Prisma client singleton
│   │   ├── auth.ts               # Auth helpers (session, JWT)
│   │   └── utils.ts              # Common utilities
│   │
│   └── types/
│       └── index.ts              # TypeScript interfaces
│
└── public/
    └── images/

REQUIREMENTS:
1. APP ROUTER: Use app/ directory. Server Components by default. 'use client' only when needed.
2. PRISMA: Define models in schema.prisma. Use Prisma Client for all DB access.
3. API ROUTES: Use Route Handlers (route.ts) with NextRequest/NextResponse.
4. TAILWIND: Dark mode support. Modern, premium UI. Mobile-first responsive.
5. TYPESCRIPT: Strict mode. Define interfaces for all data shapes.
6. LOADING: Add loading.tsx and error.tsx for key routes.
7. SERVER ACTIONS: Use Server Actions for form mutations where appropriate.

TASK:
{prompt}
